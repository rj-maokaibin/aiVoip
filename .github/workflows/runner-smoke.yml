name: Runner Smoke

on:
  push:
    branches:
      - "agent/**"
  pull_request:
    paths:
      - ".github/workflows/runner-smoke.yml"

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner reached workflow steps
        run: echo "runner-smoke-ok"
