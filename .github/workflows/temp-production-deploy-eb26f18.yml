name: TEMP Production Deploy eb26f18 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: [self-hosted, linux]
    steps:
      - name: Disabled marker
        run: echo "Temporary production deploy workflow is disabled."
