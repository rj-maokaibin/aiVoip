name: AI E1-E6 + M7 + Evidence Report Gate

# Product branches keep this workflow opt-in. The ci/pr7-controlled-gate branch
# temporarily enables pull_request execution against the PR7 branch so the
# cumulative PR3-PR7 software gate can converge on the controlled Linux runner.
on:
  workflow_dispatch:
  pull_request:
    branches:
      - feat/pr7-diagnostic-contracts

concurrency:
  group: pr7-controlled-gate-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ai-contracts:
    runs-on:
      - self-hosted
      - linux
      - x64
      - voip-controlled-linux
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Controlled Linux environment
        run: |
          uname -a
          python3 --version
          docker --version
          docker info
          node --version
          npm --version
          git rev-parse HEAD
      - name: Run VOIP AI software release gate
        run: bash tools/voip_ai_release_gate.sh
      - name: Upload validation artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: voip-ai-pr3-pr7-validation
          path: validation/**
          if-no-files-found: warn
          retention-days: 14
