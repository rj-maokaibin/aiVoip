{
  "sftp_fail_before_get_count": 0,
  "sftp_fail_after_get_count": 0,
  "persist_fail_count": 1,
  "metadata": {
    "gate": "R3-04",
    "mode": "AFTER_DURABLE_BEFORE_DB",
    "purpose": "Persist the exact segment durably, then inject one failure before the DB PERSISTED commit. Retry must reuse the immutable segment/object and eventually ACK/delete safely."
  }
}
